<!DOCTYPE html>
<html>
<head>
  <title>Edit Family</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<div class="container mt-4">

  <!-- Top X Button -->
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h4>Edit Family Member</h4>
    <a href="/admin/dashboard" class="btn btn-danger btn-sm">X</a>
  </div>



  <form class="card p-3" method="POST" action="/update-family/<%= family.id %>">
    <div class="mb-2">
      <label class="form-label">Name</label>
      <input class="form-control" name="name" value="<%= family.name %>" required>
    </div>

    <div class="mb-2">
      <label class="form-label">Mobile</label>
      <input class="form-control" name="mobile" value="<%= family.mobile %>" required>
    </div>

    <div class="mb-2">
      <label class="form-label">Email</label>
      <input class="form-control" name="email" value="<%= family.email %>" required>
    </div>

    <div class="mb-2">
      <label class="form-label">Occupation</label>
      <input class="form-control" name="occupation" value="<%= family.occupation %>">
    </div>

    <div class="mb-2">
      <label class="form-label">Door No</label>
      <input class="form-control" name="door_no" value="<%= family.door_no %>">
    </div>

    <div class="mb-2">
      <label class="form-label">Street</label>
      <input class="form-control" name="street" value="<%= family.street %>">
    </div>

    <div class="mb-2">
      <label class="form-label">District</label>
      <input class="form-control" name="district" value="<%= family.district %>">
    </div>

    <div class="mb-2">
      <label class="form-label">State</label>
      <input class="form-control" name="state" value="<%= family.state %>">
    </div>

    <div class="mb-2">
      <label class="form-label">Pincode</label>
      <input class="form-control" name="pincode" value="<%= family.pincode %>">
    </div>

    <div class="mb-2">
      <label class="form-label">Date of Birth</label>
      <input type="date" class="form-control" name="dob" value="<%= family.dob.toISOString().split('T')[0] %>">
    </div>

    <div class="mb-2">
      <label class="form-label">Gender</label>
      <select class="form-control" name="gender">
        <option value="Male" <%= family.gender === 'Male' ? 'selected' : '' %>>Male</option>
        <option value="Female" <%= family.gender === 'Female' ? 'selected' : '' %>>Female</option>
        <option value="Other" <%= family.gender === 'Other' ? 'selected' : '' %>>Other</option>
      </select>
    </div>

  

    <button class="btn btn-success">Update</button>

  </form>
</div>

</body>
</html>
