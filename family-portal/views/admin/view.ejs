<!DOCTYPE html>
<html>
  <head>
    <title>View Family Member</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container mt-4">
      <h4>Family Member Details</h4>

      <div class="card p-4 shadow-sm">
        <p><b>Name:</b> <%= member.name %></p>
        <p><b>Mobile:</b> <%= member.mobile %></p>
        <p><b>Email:</b> <%= member.email %></p>
        <p><b>Occupation:</b> <%= member.occupation %></p>
        <p>
          <b>Address:</b>
          <%= member.door_no %>, <%= member.street %>, <%= member.district %>,
          <%= member.state %> - <%= member.pincode %>
        </p>
        <p>
          <b>Date of Birth:</b> <%= member.dob ? member.dob.toDateString() :
          'N/A' %>
        </p>
        <p><b>Gender:</b> <%= member.gender %></p>
        <p><b>Parent ID:</b> <%= member.parent_id || 'N/A' %></p>

        <a href="/admin/dashboard" class="btn btn-secondary mt-3">Back</a>
      </div>
    </div>
  </body>
</html>
