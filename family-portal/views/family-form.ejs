<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><% if (addChildMode) { %>Add Child<% } else { %>Add Family<% } %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="/js/family.js"></script>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

  <!-- Brown Theme -->
  <style>
    :root {
      --brown-main: #6f4e37;
      --brown-light: #f5efe9;
      --brown-border: #d6c2b3;
    }

    body {
      background-color: var(--brown-light);
    }

    h3, h5 {
      color: var(--brown-main);
      font-weight: 600;
    }

    .card {
      border: 1px solid var(--brown-border);
      border-radius: 10px;
      margin-bottom: 15px;
    }

    .btn-primary {
      background-color: var(--brown-main);
      border-color: var(--brown-main);
    }

    .btn-primary:hover {
      background-color: #5a3f2c;
      border-color: #5a3f2c;
    }

    .form-control:focus {
      border-color: var(--brown-main);
      box-shadow: 0 0 0 0.2rem rgba(111, 78, 55, 0.25);
    }

    .img-thumbnail {
      max-height: 150px;
      object-fit: cover;
      display: none;
      margin-top: 8px;
    }
  </style>
</head>
<body class="container-fluid mt-4">

<div class="d-flex justify-content-between align-items-center mb-4 flex-wrap">
  <h3 class="mb-3 mb-md-0"><% if (addChildMode) { %>Add Child<% } else { %>Add Family<% } %></h3>
</div>

<% if (addChildMode) { %>
<form method="POST" action="/add-child" enctype="multipart/form-data">
<% } else { %>
<form method="POST" action="/save-family" enctype="multipart/form-data">
<% } %>

<% if (addChildMode) { %>
<div class="row g-2">
  <div class="col-12">
    <div class="card p-3">
      <h5 class="mb-3 small">Child Details</h5>
      <div class="row g-2">
        <div class="col-12 col-md-6">
          <input class="form-control mb-2 small" name="name" placeholder="Child Name" required>
          <input type="date" class="form-control mb-2 small" name="dob">
          <select class="form-control mb-2 small" name="gender">
            <option value="">Select Gender</option>
            <option value="male">Male</option>
            <option value="female">Female</option>
            <option value="other">Other</option>
          </select>
        </div>
        <div class="col-12 col-md-6">
          <input class="form-control mb-2 small" name="occupation" placeholder="Occupation">
        </div>
      </div>

      <h5 class="mt-4 mb-3 small">Photo</h5>
      <input type="file" class="form-control small" name="photo" accept="image/*" onchange="handlePhotoSelect(this, 'child_preview')">
      <img id="child_preview" class="img-thumbnail">

      <div class="mt-4">
        <button type="submit" class="btn btn-primary w-100 w-lg-auto" style="min-height: 44px; font-size: 16px;">Add Child</button>
      </div>
    </div>
  </div>
</div>
<% } else { %>
<div class="row g-2">
  <div class="col-12">
    <div class="card p-3">
      <div class="row g-2">
        <div class="col-12 col-md-6">
          <h5 class="mb-3 small">Parent Details</h5>
          <input class="form-control mb-2 small" name="parent[name]" placeholder="Husband Name" required>
          <input class="form-control mb-2 small" name="parent[wife_name]" placeholder="Wife Name">
          <input class="form-control mb-2 small" name="parent[mobile_wife]" placeholder="Wife Mobile" type="tel">
          <input class="form-control mb-2 small" name="parent[occupation_wife]" placeholder="Wife Occupation">
          <input class="form-control mb-2 small" name="parent[mobile]" placeholder="Husband Mobile" type="tel">
          <input class="form-control mb-2 small" name="parent[occupation]" placeholder="Husband occupation">
        </div>

        <div class="col-12 col-md-6">
          <h5 class="mb-3 small">Photos</h5>
          <div class="mb-3">
            <label class="form-label small">Husband Photo</label>
            <input type="file" class="form-control small" name="parent[husband_photo]" accept="image/*" onchange="handlePhotoSelect(this, 'husband_preview')">
            <img id="husband_preview" class="img-thumbnail">
          </div>
          <div class="mb-3">
            <label class="form-label small">Wife Photo</label>
            <input type="file" class="form-control small" name="parent[wife_photo]" accept="image/*" onchange="handlePhotoSelect(this, 'wife_preview')">
            <img id="wife_preview" class="img-thumbnail">
          </div>
        </div>
      </div>

      <h5 class="mt-4 mb-3 small">Address</h5>
      <div class="row g-2">
        <div class="col-12 col-md-6">
          <input class="form-control mb-2 small" name="parent[door_no]" placeholder="Door No">
          <input class="form-control mb-2 small" name="parent[street]" placeholder="Street">
        </div>
        <div class="col-12 col-md-6">
          <select class="form-control mb-2 small" name="parent[state]" id="state">
            <option value="">Select State</option>
          </select>
          <select class="form-control mb-2 small" name="parent[district]" id="district">
            <option value="">Select District</option>
          </select>
        </div>
      </div>

      <input class="form-control mb-3 small" name="parent[pincode]" placeholder="Pincode" type="text">

      <h5 class="mt-4 mb-3 small">Children</h5>
      <div id="children" class="mb-3"></div>
      <button type="button" class="btn btn-secondary btn-sm mb-3" onclick="addChild()">+ Add Child</button>

      <div class="mt-4">
        <button type="submit" class="btn btn-primary w-100 w-lg-auto" style="min-height: 44px; font-size: 16px;">Save Family</button>
      </div>
    </div>
  </div>
</div>
<% } %>

</form>

<script>
  function handlePhotoSelect(input, previewId) {
    if(input.files && input.files[0]){
      const reader = new FileReader();
      reader.onload = function(e){
        const img = document.getElementById(previewId);
        img.src = e.target.result;
        img.style.display = 'block';
      }
      reader.readAsDataURL(input.files[0]);
    }
  }
</script>

</body>
</html>
